// Calculate the nth Fibonacci number
num fib(num n) {
    if (n <= 1) {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
}

// Example usage
num result;
result := fib(10);
ptline(result);
